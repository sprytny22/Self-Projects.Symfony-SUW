{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {% for message in app.flashes('') %}
            <div class="col-lg-4">
                <div class="alert alert-success">
                    {{ message }}
                </div>
            </div>
        {% endfor %}
        {% if users %}
            <div class="col-lg-12 table-wrapper-scroll-y my-custom-scrollbar">
                <table class="table table-bordered table-striped mb-0">
                    <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Imie</th>
                        <th scope="col">Nazwisko</th>
                        <th scope="col">Numer indeksu</th>
                        <th scope="col">Akcja</th>

                    </tr>
                    </thead>
                    <tbody>
                    {% for us in users %}
                        <tr>
                            <th scope="row">{{ us.id }}</th>
                            <td>{{ us.username }}</td>
                            <td>{{ us.surName }}</td>
                            <td>{{ us.indNumber }}</td>
                            <td>
                                <a href="/students/passwd/{{ us.id }}" class="btn btn-info" role="button">Zmień hasło</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}